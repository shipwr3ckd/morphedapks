[project]
name = "uni-apks"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.13"
authors = [
    { name = "krvstek" }
]
license = { file = "LICENSE" }
urls = { Repository = "https://github.com/krvstek/uni-apks" }
dependencies = [
    "beautifulsoup4>=4.15.0",
    "curl-cffi>=0.16.3",
]

[tool.ruff]
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "A", "ISC", "UP", "RUF", "SIM", "C4", "PTH", "G", "PIE", "RSE"]
ignore = ["E501"]

[tool.pyright]
pythonVersion = "3.13"
pythonPlatform = "Linux"
include = ["src", "main.py"]
typeCheckingMode = "strict" 